on:
  workflow_dispatch:

jobs:
  check-unused-dep:
    name: Check For Unused-Dependency Commits
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: latest

    - name: Install dep-scimitar
      run: npm install dep-scimitar

    - name: Run dep-scimitar
      run: |
        unusedDepChange=$(npx dep-scimitar runremote)
        echo "UnusedDepCommit=$unusedDepCommit" >> $GITHUB_ENV
        echo "UnusedDepCommit: ${{ env.UnusedDepCommit }}"
